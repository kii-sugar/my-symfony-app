<style>
  .A {
    background: #ddd;
  }
  .B {
    background: #dff;
  }
</style>
<table>
  <tr>
    <th>Name</th>
    <th>Age</th>
    <th>Mail</th>
  </tr>
  {% set flg = true %}
  {% for item in data %}
    <tr class="{{ flg ? 'A' : 'B' }}">
      <td>{{item.name}}</td>
      <td>{{item.age}}</td>
      <td>{{item.mail}}</td>
    </tr>
    {% set flg = not(flg) %}
  {% endfor %}
</table>